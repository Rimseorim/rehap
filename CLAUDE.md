# 이 프로젝트의 목표

**CrossFit Rehab Guide** — 크로스핏 통증 재활 가이드 웹 서비스

전체 기획서: `docs/CrossFit 재활가이드 원페이지 기획서.md`
디자인 스펙: `docs/DESIGN.md`
상세 기획서: `docs/재활_기획서_260503.docx`

---

## 한 줄 목표

크로스피터가 박스에서 스마트폰으로 5분 안에 통증 원인을 감별하고 맞춤 재활 루트를 받을 수 있는 웹을 만든다.

## 핵심 원칙 (작업 시 항상 기준으로 삼을 것)

1. **원인 먼저, 운동 나중** — 통증 부위 선택 즉시 운동을 주지 않는다. 감별 후 루트 배정.
2. **이유를 붙인다** — 모든 운동 처방에 "왜 이 운동인가" 포함. 운동 목록이 아니라 설명.
3. **단계는 작게** — 맨몸→PVC→빈 바→바벨 순서. 현재 단계가 항상 보여야 한다.
4. **5분 완주 설계** — 1화면 1질문, 진행 바, 뒤로 가기 상태 유지.
5. **위험 신호는 즉시 멈춤** — 병원 권유 조건 감지 시 재활 루트 없이 병원 안내로 전환.

## v1 범위

- 동작: 스쿼트 3종 (백스쿼트·프론트스쿼트·오버헤드스쿼트)
- 각 동작 × 통증 부위 → 원인(태그+진단명으로 구분) 분기 → 6단계 재활 루트
  - 화면에는 "원인 A/B/C" 같은 내부 분류 글자 노출 안 함 — 사용자가 의미를 알 수 없으므로 태그(구조적/과부하 등) + 진단명(예: 슬개대퇴 증후군)만 표시
- 복합 원인 처리: 구조적 원인(가동성) → 과부하 순 우선

## 성공 기준 (출시 3개월)

- 감별 완료율 60% 이상
- 재활 루트 진입율 40% 이상
- MAU 200명

---

## 프로젝트 구조

```
01.test/
├── index.html          # 앱 메인 (BUNDLED 데이터 포함, 이게 실제 서비스)
├── backend/            # FastAPI 백엔드 (auth, records)
├── data/               # 운동 데이터 JSON (현재 앱에서 미사용, BUNDLED 데이터 우선)
├── scripts/            # 데이터 빌드 스크립트
├── docs/               # 기획서·디자인 스펙
├── Procfile            # Railway 배포용
└── requirements.txt
```

## 배포

- **플랫폼**: Railway (`motivated-prosperity` 프로젝트)
- **레포**: `github.com/Rimseorim/rehap` (main 브랜치 push → 자동 배포)
- **URL**: `web-production-28002.up.railway.app`
- main 브랜치에 push하면 Railway가 자동 배포

## 주요 결정사항

- 앱 데이터는 `index.html` 내부 BUNDLED 데이터 사용. `data/rehab.json`은 미사용.
- recovery_test 체인(elbow 등): retestMode에서 pass_next 무시하고 outcome만 처리 (의도된 동작)
