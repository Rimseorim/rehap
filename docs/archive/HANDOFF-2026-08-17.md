# HANDOFF - 2026-08-17

## 완료
- task #17 SFMA2 응급신호 danger 카드 도입 — 원인 확정(`cause()`) 화면 danger 카드에 어깨(회전근개 파열)·발목(DVT)·허리(마미증후군) 3개 문구를 pain site별 조건부로 추가. `EXTRA_DANGER_SIGNS` 매핑 + `dangerSignItem()` 헬퍼로 기존 2개 공통 항목(부종/열감)과 함께 렌더링. 스펙: `docs/superpowers/specs/2026-08-17-danger-card-sfma2-signals-design.md`, 계획: `docs/superpowers/plans/2026-08-17-danger-card-sfma2-signals.md`. subagent-driven-development로 구현(2-task), spec 리뷰·코드품질 리뷰·최종 통합 리뷰 전부 통과(이슈 없음), Playwright 브라우저 검증 12/12 통과(실제 BUNDLED 데이터 기반 경로, shoulder/ankle/lower-back 노출·knee 미노출 확인). 커밋 `e38d8e9`~`44d619f`, push 완료(`44d619f`, origin/main 반영, 자동배포 트리거됨).
- 브레인스토밍 단계에서 YBT 좌우비대칭 항목은 기존 재평가 체크리스트("가동 범위가 통증 없는 쪽과 동일하거나 더 나온다")와 중복으로 판단해 폐기. 고관절 FAI·무릎 인대/반월판·발목 하이 안클스프레인은 유발 자세 필요(문구만으론 위양성 심각)로 폐기, Neer/Hawkins는 test 노드 후보로 백로그 분리.

## 진행중
(없음)

## 대기 (task 목록, 완료분 제외) — 총 11개

**신규 리서치 (오늘 세션에서 발견)**:
- 손목·팔꿈치·흉근 위험신호 리서치 — SFMA2 참고자료에 없어 이번엔 스킵, 필요시 새 웹서치/매뉴얼 검토 필요

**설계 필요**:
- (기존 #17 안내문구 정책은 완료 처리됨)

**큰 작업 / 후순위**:
- #6 긍정적 언어 사용 원칙 전체 재검토 (index.html 전수 조사)
- #7 캐리 철학 검증
- #10 감별질문 전체 애매성 스캔
- #14 모터컨트롤 자가측정 문항 재설계

**문서화**:
- #4 3R 리인포스 — why 필드 근거 보강 (콘텐츠 작성 시 참고)
- #5 YBT 상지버전 — 운동복귀 체크리스트 후보 (단, 하지버전은 #17에서 재평가 체크리스트와 중복 판단으로 폐기됐으니 상지버전도 같은 기준으로 재검토 필요)
- #8 임상 근거 통합 문서 작성 검토

**최종 논의(제일 마지막)**:
- #18 복합 원인 후보 배정 우선순위 로직

**신규**:
- #20 기존 stage-3(운동복귀) 팁 483개에 구체 운동 인라인 백필 — 정책만 확정, 미착수

## 결정사항 / 주의
- `.claude/settings.local.json` 로컬 변경분은 계속 커밋 안 함 (관례 유지)
- task 진행 순서는 번호 순이 아니라 난이도/준비도 우선순위 그룹 순
- **danger 카드 문구 선정 기준(이번 세션에서 확정)**: "원인 확정 화면엔 동작검사 없음, 정적 문구만" 원칙 하에, 유발 자세 없이도 위양성 낮은 항목(객관적 관찰 가능하거나 정상 범위에서 거의 안 나타나는 극적 증상)만 채택. 유발 자세가 있어야 판별되는 항목(고관절 굴곡+내회전, 발목 조임/외회전 스트레스 등)은 문구화하면 특이도가 없어져 크로스핏 참여자 대부분이 해당되는 위양성 문제 발생 — 이런 항목은 test 노드로만 다뤄야 함. 다음에 비슷한 자가진단 문구를 추가할 때 이 기준을 재사용할 것.
- **사용자 피드백**: "자가진단 검사보다 문구가 낫다" — 새 위험신호 콘텐츠를 넣을 때 인터랙티브 질문/검사보다 정적 안내 카드를 우선 검토할 것. YBT처럼 이미 구현된 것과 중복인지도 먼저 확인.
- 이 프로젝트는 main 브랜치에 직접 커밋하는 관례 (feature 브랜치 없음).
- Playwright는 Node에 설치돼 있음(`node_modules/playwright`) — 스크립트는 반드시 프로젝트 루트 안에서 실행(모듈 resolve 문제), `page.content()`가 아니라 `#main-screen` innerHTML로 검증해야 함(BUNDLED 원본 JS가 항상 페이지에 포함돼 있어 `page.content()`는 위양성 발생).

## 다음 세션 권장 첫 프롬프트
/resume
