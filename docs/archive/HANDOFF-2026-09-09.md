# HANDOFF - 2026-09-09 00:00

## 완료
- 진행률 바 뒤로가기(goBack) 버그 수정 — question/test/cause 구간에서 뒤로 갈 때 `S.stepDepth`가 안 줄고 `S.maxPctReached` 가드도 안 풀려서, 도달했던 최고 퍼센트(예: cause 82%)에 눌어붙어 있던 문제. `6c608db`
  - 실브라우저(claude-in-chrome)로 82%→56%→43%→30%→30%→10% 역순 전체 확인 완료
  - 사용자가 실사용 중 "동작선택까지는 됨. 더 뒤로 갔을때부터 안됨"으로 직접 발견해서 보고
- 재활 홈 면책 문구 두 문장을 별도 `<p>` 문단으로 분리 (`본 서비스는 의료 진단·치료를... / 통증이 지속되거나...`). `6e4c917`
- 지난 세션 진행률 재설계(`98e390e` 등) 실사용 재확인 완료 — pain_site→movement→뒤로가기→pain_site 흐름 10%/30% 정확히 표시됨
- origin main push 완료 (`57acfa7..6e4c917`), GitHub Pages 배포 확인(200, 최신 코드 반영)
- Railway 백엔드 상태 확인 → 서비스 오프라인 확인됨 (사용자가 대시보드에서 직접 확인). 로그인 이관 예정이라 우선순위 낮음, 지금 당장 조치 불필요로 결론

## 진행중
없음

## 대기
- **PT/전문의 임상 검수** — 유일한 진짜 배포 블로커. 검수용 아티팩트(2026-08-31 세션 발행, `https://claude.ai/code/artifact/ba2c3e4d-e4d1-4dca-bfec-a0dbd8b1f4d1`) 공유 켜고 전달했는지 미확인
- **자동 이어하기 기능 재검토 여부 미정** — `init()`(`index.html:790-796`) localStorage 복원 기능이 원하는 동작인지 아직 논의 안 됨
- 실기기(모바일) 전체 흐름 완주 테스트 — 미착수 (이번 세션은 데스크톱 브라우저로만 확인)
- 이용약관/개인정보처리방침 문서화 — 로그인 이관 확정 전까진 갭으로 취급
- 카카오/구글 로그인 실연동 — 우선순위 낮음/보류
- **Railway 백엔드 재시작 여부** — 오프라인 상태 그대로 둘지, 이관 전이라도 살려둘지 미결정 (현재는 "안 켜도 됨"으로 잠정 결론)

## 결정사항 / 주의
- `goBack()`은 이제 뒤로 갈 때마다 `S.stepDepth`를 1 감소(0 하한) + `S.maxPctReached`를 0으로 리셋한다. 새로운 화면 전환 로직을 goBack 경로에 추가할 때 이 리셋을 빠뜨리면 회귀방지 가드가 다시 눌어붙는 버그가 재발한다.
- movement(30%, 고정)→챕터1 첫 질문(30%, stepDepth=0 보간값)이 값이 같은 건 의도된 설계 — 고정 구간과 보간 구간의 경계값을 일부러 맞춘 것. 버그 아님, 변경 안 하기로 확정.
- Railway(`web-production-28002.up.railway.app`)는 서비스 오프라인 상태. GitHub Pages(`rimseorim.github.io/rehap/`)만으로 핵심 서비스(진단→재활 루트) 정상 작동 확인됨. 로그인/기록저장 API만 영향받음.
- GitHub Pages와 로컬 index.html 완전 동일 확인(BOM 문자 차이만 있고 실질 diff 없음).

## 다음 세션 권장 첫 프롬프트
`/resume`
