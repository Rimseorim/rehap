# HANDOFF - 2026-09-15 15:09

## 완료
- 용어 설명 "?" 버튼 UI 제거, `BUNDLED.glossary` 데이터·`glossaryToggle()` 함수는 유지 (`9b6cefe`, push됨)
- 스쿼트 감별 테스트 21개 중 영상 없던 18개에 유튜브 링크 채움 — 웹서치로 검사명(트렌델렌버그, FADIR, Thomas test, painful arc, 슬개대퇴 압박 등)에 맞는 시연 영상 매칭 (`2753561`, **아직 push 안 함**)

## 진행중
- **테스트 화면 영상 채우기** (동작별 순차 진행 방식으로 사용자 승인받음)
  - 중단 지점: 스쿼트만 완료, 나머지 7개 동작 전부 남음
  - 남은 개수: 런지 13 · 데드리프트 21 · 풀업 15 · 키핑 14 · 로우 16 · 수직프레스 16 · 수평프레스 17 (총 112개 테스트, 순서는 manifest 순서: 스쿼트→런지→데드리프트→풀업→키핑→로우→수직프레스→수평프레스)
  - 다음 스텝: 런지부터 동일 방식(WebSearch로 검사명 매칭 영상 찾기 → JS로 BUNDLED JSON 파싱/수정/재직렬화 → git diff 1줄만 바뀌는지 확인 → 커밋)으로 이어가기. 스크립트 패턴은 스크래치패드의 `fill_squat_videos.js` 참고 (매핑 객체만 바꿔서 재사용 가능)

## 대기
- **"글귀(카피) 전체 검토"** — 사용자가 테스트·재활운동 화면 문구를 직접 다 읽고 행동 유도가 되는지 확인하겠다고 함(본인이 직접 할 작업, Claude 작업 아님)
- **PT/전문의 임상 검수** — 유일한 진짜 배포 블로커, 여전히 대기 중
- **Railway 백엔드 가동** — `backend/routers/auth.py` FRONTEND_URL 오타 수정 커밋 여부 미확정 (이전 세션 항목, 이번 세션엔 손 안 댐)
- **SQLite 영속성 문제** — 미결정 (이전 세션 항목)

## 결정사항 / 주의
- 물음표 UI 제거는 데이터(`BUNDLED.glossary`)는 남기고 화면 요소 2곳(`glossaryToggle()` 호출부)만 지우는 것으로 확정 — 데이터 다시 필요해지면 호출부만 복원하면 됨
- 영상 URL 채우기 검증법: `BUNDLED` JSON을 파싱→수정→`JSON.stringify()`로 재직렬화했을 때 원본과 100% round-trip 일치함을 확인함 (공백 없는 압축 포맷) → 이 방식으로 계속 안전하게 자동화 가능
- 룸바락(Lumbar Lock) 계열 테스트 3종(rom/core/scapula)은 동일 영상(TPI Lat Test, `5Sop_suO8uI`) 재사용 — 앱에 없는 커스텀 복합 테스트라 딱 맞는 영상이 없어서 base 동작이 같은 영상으로 통일
- id는 동작(motion) 섹션 안에서만 유일함 — 예: `test-ankle-df`가 스쿼트와 런지에 각각 다른 내용으로 존재. 전역 검색/치환 절대 금지, 반드시 `BUNDLED.<motion>` 범위 안에서만 walk할 것

## 다음 세션 권장 첫 프롬프트
`/resume`
