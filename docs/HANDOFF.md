# HANDOFF - 2026-09-22 14:00

## 완료
- 가슴 통증 원인 화면에 심폐 위험신호 문구 추가 (`EXTRA_DANGER_SIGNS.chest`를 배열화, "운동 중 가슴이 조이거나 숨이 차요 / 식은땀·어지러움·왼팔로 뻗치는 통증이 함께 있는 경우" 문구, 다른 항목과 같은 "~경우" 패턴으로 통일) (commit `d524996`, `c6652ef`)
- `cause()` 렌더링을 `EXTRA_DANGER_SIGNS`가 배열이든 단일 객체든 다 지원하도록 수정 — 다른 부위(어깨·발목·허리·팔꿈치)는 그대로 단일 객체 유지, 확장 필요 시 배열로 바꾸면 됨
- origin/main까지 push 완료

## 진행중
- 없음

## 대기
- PT/전문의 임상 검수 — 유일한 진짜 배포 블로커, 여전히 대기 중
- 위험신호 엣지케이스 전반 — 공통 체크 화면(1번) 도입은 사용자가 "취지 안 맞음"으로 명시 거부, 재제안 금지. 부위별 정적 문구 확장(2번)도 가슴 1건만 하기로 축소 완료, 무릎/고관절/손목 정적 문구 추가는 보류 상태(필요성 낮다고 판단, 안정시통증 문항으로 커버된다고 봄) — 다시 꺼내지 말 것
- "글귀(카피) 전체 검토" — 사용자 직접 예정, 아직 시작 안 함
- Railway 백엔드 FRONTEND_URL, SQLite 영속성 — 로그인 이관 예정이라 백엔드 유지/폐기 방향 결정이 먼저 필요, 미결정 이월 계속

## 결정사항 / 주의
- 위험신호 엣지케이스 논의 결과: 공통 체크 화면(부위 선택 전 레드플래그 체크리스트)은 화면·클릭을 늘려 앱 취지(5분 완주, 1화면 1질문)와 안 맞는다고 사용자가 판단해 접음. 대신 가장 위험도 높은 사각지대(가슴 부위 심폐 신호)만 기존 위험신호 박스에 한 줄 추가하는 최소 조치로 절충함. 무릎/고관절/손목 등 다른 부위 정적 문구 미비는 "안정시통증 문항으로 대부분 커버됨"으로 보고 현재는 손대지 않기로 함
- `EXTRA_DANGER_SIGNS`의 `chest`만 배열 형태(`[{title,sub}, ...]`)이고 나머지 4개 부위(shoulder/ankle/lower-back/elbow)는 여전히 단일 객체 — `cause()`가 `Array.isArray` 체크로 두 형태 다 처리하므로 기존 항목은 안 건드려도 됨. 다른 부위에 문구 추가할 일 생기면 그 부위도 배열로 바꾸면 됨
- Node 스크립트로 index.html의 `<script>` 블록별 `node --check` 문법 검증하는 방법 확립: 정규식 `/<script[^>]*>([\s\S]*?)<\/script>/g`로 각 script 블록을 분리 저장 후 개별 검증(단일 greedy 매치는 여러 script 태그가 있으면 실패함 — 이번에 시행착오로 확인)
- 문구 작성 시 기존 유사 항목들의 문장 패턴(예: sub는 항상 "~경우"로 끝나는 묘사문, 행동 지시는 박스 하단 공통 문장이 전담)을 먼저 확인하고 맞출 것 — 이번에 한 번 어긋났다가 사용자 지적으로 수정함

## 다음 세션 권장 첫 프롬프트
`/resume`
