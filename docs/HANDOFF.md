# HANDOFF - 2026-08-28 (세션 인계)

## 완료
- task #14 모터컨트롤 자가측정 문항 정도부사 제거 — 어깨 저항 테스트 3곳("힘이 많이 빠져요"→"빠져요"), Thomas 테스트 3곳("많이 당겨요"→"당겨요"), 고관절 굴곡 가동성 1곳("어느 정도 내려가면"→"끝까지 앉기 전에"), `df11159`
  - pass_text가 이분법인데 fail_text만 정도부사를 써서 기준이 어긋나던 실제 버그 7곳 수정
- task #6 (일부) 어깨 cause-case2 태그 "조절 실패"(6곳)→"조절력 부족", "신경근 조절 실패"(1곳)→"신경근 조절력 부족", `e9368de`
  - tag 필드는 `index.html:1481`(`<span class="cause-tag">`), `:1658`(결과 페이지 h1)에서 실제로 화면에 그대로 노출됨을 코드로 확인 후 수정
  - 스쿼트/데드리프트/키핑/풀업/로우/수직프레스/수평프레스 7개 동작의 어깨 통증 감별 경로에 공통 적용된 cause-case2 대상

## 진행중
- task #6 긍정적 언어 재검토 (나머지)
  - 중단 지점: `name`(진단명) 필드에 "실패" 포함 7건 — cause-case2의 `name`("코어-상지 분리 실패 (팔 들 때...)", "요추-골반-상지 신경근 조절 실패 (...)") 이 화면에 진단명으로 노출되는지 아직 미확인
  - 다음 스텝: index.html에서 `c.name` 또는 동일 cause 객체의 name 필드가 렌더링되는 위치 검색 → 노출되면 tag와 동일하게 "실패"→"부족" 계열로 교체
  - 확인 완료된 것: "안 됩니다"(7건)·"불가능"(1건)은 전부 `why`/`cue` 필드의 운동 자세 교정 지침이라 원칙 위반 아님 → 수정 불필요로 결론남

## 대기 (총 2개)
- #20 기존 stage-3(운동복귀) 팁 483개에 구체 운동 인라인 백필 — 정책만 확정, 미착수. #6과 규모 비교 완료(단순 반복량은 #20이 크고, 판단 난이도는 #6이 높음)
- #14 나머지 없음 — 스캔 결과 실질 대상 7건이 전부였고 전량 처리 완료, task #14 자체는 종료 처리 가능 (다음 세션에서 확정 필요)

## 결정사항 / 주의
- #14 스캔 시 "균형/버티기"·"쏠림/정렬"·"대칭/비교" 그룹은 이미 카운트/이분법 기준이 있어 손댈 필요 없다고 판단, "정도부사(주관)" 그룹만 실제 수정 대상이었음
- #6에서 판단 기준 확정: 사용자 상태를 "실패/기능장애"로 판정하는 진단성 표현(tag/name/진단명)만 원칙 위반 대상. "~하면 안 됩니다" 류 운동 자세 코칭 큐는 별개 범주로 원칙 위반 아님
- Thomas 테스트 관련 논의: 스쿼트 시 허리 말림 = 척추 문제가 아니라 고관절 가동성 부족의 보상(butt wink) — 테스트 설계 의도가 맞다고 결론, purpose/note는 손대지 않음
- BUNDLED 데이터 편집은 Node 스크립트로 개수 검증(split/join) 후 일괄 적용 방식 계속 사용 중, 정상 작동 확인됨

## 다음 세션 권장 첫 프롬프트
`/resume`
