# HANDOFF - 2026-09-24 19:28

## 완료
- **영상 검수 15건 정비** (push 완료, main `71a82c2..4ae3f9d`)
  - `d63675e`: 스쿼트 발목 굽힘 검사 이름을 런지와 동일하게 통일 → 세 곳 모두 "발목 굽힘(배측굴곡) 검사"
  - `4ae3f9d`: 제목 8개 규칙 반영, 런지 발목 굽힘 통과 문구·방법 2단계 합침, "양성" 문구 2곳(스쿼트 발목 굽힘·FADIR), 토마스 검사 9곳(사실관계라 중복 인스턴스 전부, "침대 면" 기준), 런지 무릎 정렬 결과·주의 문구, 균형 검사 10초 통일(스쿼트 발목 `cause-c` 다음 단계 조건 포함), 백 익스텐션 A안(초 기준 삭제)
- 영상 검수판 Artifact 최신 = v49 (`https://claude.ai/artifact/3S2juJAfWMDJG2yWoo1pDe`, 링크 공유 상태)
- **원인·루트 검수판 신규** Artifact v2: `https://claude.ai/artifact/YG6sHXLeqdYs4HzsAaYVRg` (비공개). 8동작 152개 원인 카드, 카드 1장 = 원인 설명 + 단계별 운동(기초 재활·재평가·운동 복귀), 동작 탭·미검수만 보기
- **용어사전 기능 제거**: `glossaryToggle`/`getGlossaryTerms` 코드, `BUNDLED.glossary` 51개, `docs/need/glossary_draft.md` 삭제 (이번 handoff 커밋에 포함)
- `docs/VIDEO_REVIEW_RULES.md`에 "원인명 규칙", "재활 운동 규칙" 섹션 추가 (이번 handoff 커밋에 포함)

## 진행중
- **재활 운동명 뒤 꼬리표 정리** (`index.html` 17개 반영·push 완료 `56e03ef`, 원인·루트 검수판 v3에도 반영 완료)
  - 반영: 기초 재활(Phase B) 첫 번째(b1) 9개, 두 번째(b2) 3개(키핑 손목 b·c·d "소범위 스윙"), 네 번째(b4) 5개("최종 통합 테스트"). 17개 모두 why·큐·방법에 목적이 이미 있어 why 보충 없이 이름만 정리. "허로우"→"할로우"도 index.html·data 각 6곳 수정
  - 보류: 로우 허리 원인 b의 b1("얕은 각도 (상체 45도)")·b2("깊은 각도 (상체 수평)") 짝 — 꼬리를 지우면 이름이 겹쳐 짝 단위 결정 필요
  - 범위 밖 잔여(약 29개): "— 무게 점진 복귀/도입" 계열 b4, "— 와이드 스탠스"·"— Full ROM" 등이 붙은 b2·b3, 키핑 손목 b·c·d b1 "데드 행 (매달리기만)" 등. 이번 정리에 넣을지 미결정
  - 다음 스텝: 위 보류·잔여 결정 (결정되면 index.html과 검수판 양쪽 반영)
- **데드리프트 모션 검수** 시작 전 (이월): 무릎(`test-valgus`/`test-valgus-lateral`) → 허리 → 어깨 → 손목 → 고관절 순

## 대기
- 원인명 규칙 적용: 152개 원인명 중 "배측굴곡"·"외측"·"테스트"에 걸리는 목록 정리 후 승인 받기 (스쿼트 무릎 a, 런지 무릎·발목 a/a-mild/b, 스쿼트 발목 c/d, 런지 무릎 c 등)
- "양성" 표현 약 43곳 잔존 (전체 정리 여부 미결정), 런지 발목 안정성 재검사 목적문 "외측 인대", "견갑골(어깨뼈)"→"날개뼈" 전수 변경(미실시)
- 벽 발목 가동성 운동 시작 거리 5cm(스쿼트 무릎a·허리b·발목a·어깨case4/d 등) vs 검사 8cm — 의도된 차이인지 판단 필요
- 한글/영어 운동명 통일 판단: 사용자가 유튜브·네이버 한국어 검색으로 직접 확인 후 결정 (WebSearch는 한국어 검색에 약함). 의학용어 포함 운동명(가자미근·광배근·거골·TFL 등) 개수 미집계
- 방법(how) 5요소 작성 기준은 보류 중
- 진급 기준(`progression_note`) 문구("상위 결함 검사 프로토콜(감별 진단)로 리턴…")는 Phase B 카드에 실제 렌더링됨 → 쉬운 말로 수정 필요
- 상대 검수자에게 줄 것: `VIDEO_REVIEW_RULES.md`, 영상 검수판 링크, 원인·루트 검수판(현재 비공개라 공유 설정 필요). `https://claude.ai/code/artifact/ba2c3e4d-e4d1-4dca-bfec-a0dbd8b1f4d1` 감별 로직 문서는 2026-08-29 버전이라 최신 index.html 기준 갱신 필요(미실시)
- 운동 video_url 미등록: 스쿼트 269개 중 263개 `TBD` (전체 8동작 미집계). 데드리프트 `test-valgus` 영상 미해결(이월)
- 용어사전 설계 문서 2개(`docs/superpowers/specs|plans/2026-06-08-glossary-toggle-*`)는 폐기된 기능 기록 — 삭제 여부 미정
- PT/전문의 임상 검수 — 여전히 유일한 진짜 배포 블로커. Railway 백엔드·로그인 이관 방향 미결정(이월)

## 결정사항 / 주의
- **운동명**: 표준명 그대로, 대시·괄호 뒤 목적·단계 표기는 삭제(없으면 why에 보충). "최종 통합 테스트"는 화면의 "진행 단계 N/4" 배지가 알려 주므로 삭제
- **원인명**: 쉬운 말(원어), 굳어진 진단명(~증후군 등)은 유지. "원어 검사 — 확인 대상" 형식은 검사 제목 전용
- **용어 표기**: 의학·해부 용어=쉬운 말(원어), 운동·기술 용어=용어(쉬운 말) — "사용자 대부분이 아는 쪽을 앞에", 괄호 12자 안팎·필드마다 첫 등장. "날개뼈"로 통일
- 균형 검사 기준은 눈 감고 10초로 통일 (근거: 18~39세 평균 약 13초, 5초 미만은 낙상 위험 신호, 10초 이상 유지 가능은 낮은 사망 위험과 연관). 토마스 검사 판정은 "늘어뜨린 다리의 허벅지가 침대 면에 닿는가"
- 문구·스타일은 검토 인스턴스만, 사실관계는 같은 id의 모든 인스턴스 수정
- `index.html` 수정은 한 줄 minified라 Edit 불가 → python으로 `BUNDLED` JSON 파싱 후 수정·재직렬화 (라운드트립 동일 검증함). 이번 세션 스크립트는 세션 임시 폴더에 있어 사라졌을 수 있음, 필요하면 다시 작성. bash heredoc에 백슬래시 있으면 훅이 막으므로 스크립트는 Write 도구로 파일 생성
- 검수판 Artifact 게시 시 "최신 버전 read → 전체 파일 Read" 뒤 publish해야 함. 체크 상태는 사용자 브라우저 localStorage라 Claude가 읽을 수 없음(사용자가 목록을 알려줘야 함)
- push는 main 자동 배포. `4ae3f9d`까지 push됨, 이후 handoff 커밋은 아직 push 안 함
- 사용자가 "작업하지 말고 대답"이라고 하면 도구 없이 답만. docs 규칙 문서는 채팅 초안 확인 후 작성이 원칙(이번엔 사용자 지시로 바로 쓴 적 있음)

## 다음 세션 권장 첫 프롬프트
`/resume`
