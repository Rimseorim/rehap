# HANDOFF - 2026-09-23 (세션 인계)

## 완료
- 영상 검수(motion-by-motion, site-by-site) 진행 중 발견된 이슈 전부 수정 — **스쿼트 파트(무릎·허리·발목·어깨·고관절) 완료**
  - 스쿼트/발목 `test-ba-ankle-df` 벽 거리: 5cm → 8cm (스쿼트/무릎의 동일 테스트와 통일, 임상 문헌 기준 근거 확보)
  - 스쿼트/어깨 `test-shoulder-arc`(통증 호 검사): "60~120도 구간" 각도 표기 → 관찰 패턴 언어로 전면 재작성(purpose/steps/passText/failText)
  - 스쿼트/고관절 `test-hip-fadir`(FADIR): name·steps 방향("발끝을 안으로"→"바깥쪽으로", 실제 바이오메카닉 오류였음)·note·failText·video_url(`&t=45`)·purpose("앞쪽" 삭제) 전부 수정. **방향 수정과 video_url은 런지·데드리프트의 동일 테스트(`test-hip-fadir-dl` 등) 3곳 전체에 적용**, 나머지 문구는 스쿼트 인스턴스만 수정(중복 인스턴스는 스코프 밖 룰 적용)
  - 전신: 순수 중복 라벨용 괄호 표기(예: "(회외)", "(내전+내회전)" 등 9개 패턴, 총 35건) `steps` 배열에서 일괄 삭제 — 용어 병기(예: "무릎 앞쪽(슬개건)")나 정보성 부연은 보존
- 위 모든 수정을 `index.html`(BUNDLED 데이터)과 검수용 Artifact(`https://claude.ai/artifact/3S2juJAfWMDJG2yWoo1pDe`, 현재 버전 36)에 동일하게 반영·동기화 완료
- **아직 커밋 안 함** — `index.html` 수정사항 working directory에 uncommitted 상태 (`git diff --stat`: 1 file changed — 단일 minified 라인이라 diff 크기로는 안 보임, 실제로는 위 항목 전부 포함)

## 진행중
- 없음 (스쿼트 파트 검수 매듭, 다음 모션 진입 전 세션 종료)

## 대기
- 다음 세션: **런지(런지) 모션 영상 검수** 시작 예정 — 사용자가 "스쿼트→런지" 순서로 진행 중이라고 밝힘, 검수 방식은 스쿼트와 동일(모션별·부위별 대표 테스트 1개씩, 영상 vs 텍스트 대조, 이슈 발견 시 채팅에 제안 먼저 → 승인 후 index.html+Artifact 양쪽 반영)
- PT/전문의 임상 검수 — 여전히 유일한 진짜 배포 블로커
- "글귀(카피) 전체 검토" — 사용자 직접 예정, 아직 시작 안 함
- Railway 백엔드 FRONTEND_URL, SQLite 영속성 — 로그인 이관 방향 미결정, 이월 계속
- **index.html 변경사항 커밋 필요** (다음 세션 시작 시 우선 처리 권장)

## 결정사항 / 주의
- 검수 워크플로우 확정: 문구/스타일 수정은 검토 중인 그 인스턴스에만 적용(다른 동작의 동일 테스트 중복 인스턴스는 안 건드림), 단 사실관계·해부학적 오류(방향 오류, 영상 링크 등)는 전체 중복 인스턴스에 동일 적용
- 모든 문구 변경은 채팅에 먼저 제시 → 사용자 승인 후에만 파일 반영 (직접 쓰기 금지 룰 계속 유효)
- "검수 완료"는 제목만 확인하고 선언 금지 — 본문(steps/note/passText/failText) 전체를 실제로 확인·수정했을 때만 완료로 표시 (이번 세션 중 사용자가 직접 지적한 사항)
- Artifact는 index.html의 수동 미러이므로 매번 최신 버전 fetch→전체 Read→Edit→publish 순서를 지켜야 함(퍼블리시 거부 방지)
- FADIR 관련 의학 용어(FAI=대퇴비구 충돌증후군) 및 발끝 방향은 WebSearch로 임상 문헌 대조 검증 완료

## 다음 세션 권장 첫 프롬프트
`/resume`
