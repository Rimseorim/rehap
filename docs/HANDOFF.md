# HANDOFF - 2026-09-18 (세션 종료 시점)

## 완료
- 영상 검수 진행 (사용자 직접, UI 순서대로) — 스쿼트 무릎 그룹 전체 완료:
  - `test-single-leg`(트렌델렌버그): 새 영상(`-wNf2bsQfrk`, 전신 노출) 교체, purpose "외전근" → "골반을 옆에서 잡아주는 근육(중둔근) 힘"으로 쉬운 말 수정
  - `test-knee-pf-provocation`(스쿼트) + 중복 인스턴스 4건(`test-knee-pf-provocation-lunge`, `test-quad-overload-retest`, `test-knee-pf-provocation-deadlift`, `test-pf-compression-retest`): 이름을 "디클라인 싱글레그 스쿼트" → "디클라인 스텝다운 테스트"로 정정(실제 검사는 20도 경사 스텝다운 테스트였음), steps를 15cm 평평한 계단/박스 + 옆으로 내리기(Lateral Step-Down Test, 접근성 개선)로 변경, 영상 `sSCKl_cbzuY`로 통일, 용어는 "무릎 앞쪽(슬개건)"으로 통일(처음 "슬개골 아래(슬개건)"로 갔다가 되돌림 — 슬개골도 어려운 한자어라 판단)
  - `test-knee-volume-overload-retest`(싱글레그 디클라인 스쿼트 재검사): 25도 경사 보드 대신 "책 위에 뒤꿈치만 올리기"로 접근성 개선한 steps로 수정 (진짜 디클라인 보드 없어도 가능)
- 스쿼트 허리 그룹 2건 완료:
  - `test-lbs-hip-flex`(고관절 굴곡 테스트): 벽 잡고 깊이 앉기(비표준, 발목·흉추 혼입) → 누워서 무릎 당기기 + 주먹 개수 측정(표준 Active Hip Flexion ROM, 발목·흉추 배제해 더 특이적)으로 전면 교체. 영상 9분짜리라 2:25(145초)부터 재생되도록 타임스탬프 기능 신규 구현(index.html `openVideoModal` + Artifact `toEmbedUrl` 둘 다 `&t=초` 파싱해서 `&start=`로 임베드에 전달하도록 수정 — 앞으로 다른 검사에도 재사용 가능한 공통 기능). purpose는 "고관절의 가동성을 확인합니다"로 단순화
  - `test-lbs-plank`(플랭크 안정성 테스트): "플랭크"→"백 익스텐션 지구력 테스트"로 완전 교체. 근거: 플랭크는 anti-extension(처짐 방지) 근지구력이라 이 원인 체인(cause-a: 고관절 가동성부족→허리 굴곡/말림 보상)이 다루는 실패 패턴과 반대 방향, 백익스텐션(척추기립근)이 더 적합. Biering-Sørensen 정식 검사는 벤치+파트너 필요해 접근성 문제 → "다리는 바닥에 그대로 두고 상체만 살짝 들어 버티기"로 최종 확정(둔근·햄스트링 개입 최소화, 장비 불필요). 컷오프 30초. 영상은 `eHbdjqkwvks`(50초 지점부터 재생)로 최종 확정. steps에 "무리하게 높이 들지 마세요" 안전 문구 포함(과신전 방지)
- 위 전체 변경사항 `index.html` BUNDLED + 영상 검수 Artifact(https://claude.ai/artifact/3S2juJAfWMDJG2yWoo1pDe, 현재 **Version 29**) 양쪽 동기화 완료

## 진행중
- **영상 검수 (사용자 직접, UI 순서대로 진행 중)**
  - 중단 지점: 스쿼트 허리 그룹 → `test-lbs-plank` 완료, 다음은 `test-tspine-extension`(흉추 신전 가동성 검사, Heel Sit T-spine Extension)부터
  - 다음 스텝: 사용자가 이어서 검토, Claude는 매칭 오류·문구 문제 보고받으면 index.html + Artifact 양쪽에 동일 수정 반영 (반영 후 반드시 Artifact도 republish까지 확인 — 이번 세션에 index.html만 고치고 Artifact 안 챙긴 실수 1회 있었음)

## 대기
- **PT/전문의 임상 검수** — 유일한 진짜 배포 블로커, 여전히 대기 중
- **"글귀(카피) 전체 검토"** — 사용자 직접 예정, 아직 시작 안 함
- **Railway 백엔드 FRONTEND_URL 오타 수정 커밋 여부** — 미확정 (여러 세션째 이월)
- **SQLite 영속성 문제** — 미결정 (여러 세션째 이월)
- **로컬 커밋 push 여부** — origin/main 대비 10커밋 앞섬(25b134d까지), 이번 세션 index.html 수정분도 아직 커밋 안 됨 (working tree에 남음)

## 결정사항 / 주의
- **작업 진행 전 확인 원칙**: 사용자가 "~하자/~해야지" 식으로 말해도 방향성 확인 차원이면 바로 실행하지 말고 먼저 대답부터 하고 컨펌받을 것 (이번 세션에 "바로 바꾸지말고 대답부터 하라고" 피드백 받음)
- 영상 검수 시 발견되는 텍스트/데이터 오류는 발견 즉시 **index.html BUNDLED**와 **검수 Artifact 양쪽 모두** 수정 — Artifact DATA는 index.html에서 직접 재생성하는 게 아니라 이제는 개별 Edit로 동기화하는 방식으로 굳어짐 (건별로 두 파일 각각 Edit 후 Artifact republish)
- 영상이 길 때(수 분 이상) 타임스탬프(`&t=초`) 넣는 기능을 신규 구현함 — 앞으로 검수 중 "영상이 몇 분짜리인데 필요한 구간이 늦게 나온다" 케이스 발견 시 이 기능으로 바로 해결 가능
- 검사 용어 통일 기준: "슬개골 아래"보다 "무릎 앞쪽(슬개건)"이 사용자 입장에서 더 쉬운 표현으로 최종 채택됨 (슬개골도 어려운 한자어)
- id는 동작(motion) 섹션 안에서만 유일 — 전역 검색/치환 금지, 반드시 `BUNDLED.<motion>` 범위 안에서만 처리 (계속 준수)
- 같은 물리적 검사가 같은 motion 내 여러 site(예: 스쿼트의 무릎/발목)에 각각 다른 id·다른 purpose로 중복 등록돼 있음 — 런지 2건·스쿼트 `test-ba-ankle-df` 1건(족관절 배측굴곡, 8cm 기준 미반영)은 여전히 미반영 상태로 남아있음 — 사용자가 해당 항목 검수할 때 처리 예정
- 검수 진행 순서는 앱 UI 순서(=manifest 순서) 그대로: 스쿼트→런지→데드리프트→풀업→키핑→로우→수직프레스→수평프레스, 각 동작 내 site 순서도 화면 그대로. 스쿼트 안에서는 무릎→허리→발목→어깨→고관절 순서

## 다음 세션 권장 첫 프롬프트
`/resume`
