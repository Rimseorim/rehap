# HANDOFF - 2026-08-11 (시각 미기재, 세션 인계 직전)

## 완료
- task #1 발목 원인 3분류 반영 (스쿼트 cause-e, 런지 cause-d 신설 + Phase A/B + Playwright 실측 검증) — 커밋 a7ab71b, c76ec97(라우팅 버그 수정)
- task #2 스쿼트 원인 판정 순서 재정렬 — 이미 SFMA 우선순위(굴곡→회전)와 일치함을 확인, 변경 불필요로 종결
- task #9 운동복귀 팁 원인맞춤 운동 포함 여부 — 정책 확정(신규/수정 cause부터 1~2개 인라인 운동 포함), 기존 483개는 일괄 재작성 안 함. 메모리에 정책 기록
- task #20(신규 등록) 기존 stage-3 팁 483개 백필 — 미착수 backlog로 별도 등록 (메모리: `project_task20_stage3_tips_backfill.md`)
- task #11+#15 통합 데드리프트-허리 ASLR/코어 재검사 로직 — 커밋 547fbc4
- task #12 수평프레스/로우 허리 코어안정성 진단 보강 — 커밋 d48026c

## 진행중
(없음 — 위 항목까지 전부 커밋·푸시 완료된 상태에서 세션 종료)

## 대기 (task 목록, 완료분 제외)

**바로 진행 가능**(설계 방향 이미 논의됨) — 남은 것:
- **#19 조인트바이조인트 게이트 질문** — 무릎(-발목)부터 시작, 허리(-고관절)는 #15와 통합(→ #15 완료로 사실상 겹치는 부분 해소됨, 재검토 필요), 어깨(-흉추)는 후순위. **다음 세션 시작점으로 추천**.

**설계 필요(시간 더 듦)**:
- #3 신호등(Red/Yellow/Green) 8동작 매핑 발췌 (FMS L2 p83-87 기준)
- #13 감별 결과 카드 Red/Yellow/Green 색상 UI (#3 완료 후 진행)
- #16 SFMA 상지패턴 거울 자가측정 문항 설계
- #17 안내문구/보조신호 배치 정책 설계 (SFMA2 응급신호 + YBT 좌우비대칭 신호 노출 위치)

**큰 작업 / 후순위**:
- #6 긍정적 언어 사용 원칙 전체 재검토 (index.html 전수 조사)
- #7 캐리 철학 검증
- #10 감별질문 전체 애매성 스캔
- #14 모터컨트롤 자가측정 문항 재설계

**문서화**:
- #4 3R 리인포스 — why 필드 근거 보강 (콘텐츠 작성 시 참고)
- #5 YBT 상지버전 — 운동복귀 체크리스트 후보
- #8 임상 근거 통합 문서 작성 검토

**최종 논의(제일 마지막)**:
- #18 복합 원인 후보 배정 우선순위 로직

**신규**:
- #20 기존 stage-3(운동복귀) 팁 483개에 구체 운동 인라인 백필 — 정책만 확정, 미착수

## 결정사항 / 주의
- `.claude/settings.local.json` 로컬 변경분은 계속 커밋 안 함 (관례 유지)
- **task 진행 순서는 번호 순이 아니라 난이도/준비도 우선순위 그룹 순** (바로 진행 가능 → 설계 필요 → 큰 작업 → 문서화 → 최종논의). 이번 세션에 다시 확인받음 — 재질문 불필요.
- **작업 검증 방식**: 이번 세션부터 데이터 수정 후 Playwright(`node_modules`에 이미 설치됨, `npx playwright install chromium` 완료)로 실제 UI 플로우를 클릭까지 해서 검증하는 패턴 확립. 임시 스크립트는 `verify_*.tmp.js`로 프로젝트 루트에 만들고 검증 후 삭제. 스크래치패드가 아니라 프로젝트 루트에서 실행해야 로컬 `node_modules/playwright` 인식됨.
- **cause 추가 시 주의**: `test-*.fail_next`/`pass_next`를 `"q:qN"`으로 연결하려면 `selectTestResult()` 함수가 `q:` 프리픽스를 지원해야 함 — task #1 작업 중 버그로 발견해 index.html에 이미 패치 완료(커밋 c76ec97). 앞으로 유사 작업 시 재발 안 함.
- **UX 원칙 재확인(#12에서)**: 진단 정확도를 높이겠다고 기존의 빠른 자가판단 질문을 신체 검사로 완전히 대체하지 말 것. "5분 완주" 원칙과 충돌. 대신 애매한 응답(예: "네, 그런 것 같아요")에만 확인 검사를 끼워넣는 하이브리드 방식이 기본값.
- **cause description 수정 시 glossaryToggle() 주의**: 결과 화면에서 description 뒤에 자동으로 "?" 툴팁 버튼이 붙는 게 정상 UI임(용어사전 매칭 시). 오타로 착각하지 말 것 — 실제 오타(문장 끝 stray "?")는 JSON 데이터 자체에 있는지 별도 확인 필요.

## 다음 세션 권장 첫 프롬프트
`/resume`
